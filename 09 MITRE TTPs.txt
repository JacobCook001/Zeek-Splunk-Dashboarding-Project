<form version="1.1" theme="light">
  <label>09 - MITRE ATT&amp;CK Network TTP Dashboard</label>

  <search id="Base_Conn">
    <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$ $dst_port_include$ $dst_port_exclude$</query>
    <earliest>$time_range.earliest$</earliest>
    <latest>$time_range.latest$</latest>
  </search>

  <fieldset submitButton="true" autoRun="true">
    <input type="time" token="time_range" searchWhenChanged="false">
      <label>Time Range</label>
      <default>
        <earliest>-24h@h</earliest>
        <latest>now</latest>
      </default>
    </input>
    <input type="multiselect" token="src_ip_include" searchWhenChanged="false">
      <label>Source IP - Include</label>
      <choice value="*">All</choice>
      <fieldForLabel>id.orig_h</fieldForLabel>
      <fieldForValue>id.orig_h</fieldForValue>
      <search>
        <query>index=zeek source="*conn.log" | table id.orig_h | dedup id.orig_h | sort id.orig_h</query>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </search>
      <default>*</default>
      <initialValue>*</initialValue>
      <valuePrefix>id.orig_h="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <prefix>(</prefix>
      <suffix>)</suffix>
    </input>
    <input type="multiselect" token="src_ip_exclude" searchWhenChanged="false">
      <label>Source IP - Exclude</label>
      <choice value="__none__">None</choice>
      <fieldForLabel>id.orig_h</fieldForLabel>
      <fieldForValue>id.orig_h</fieldForValue>
      <search>
        <query>index=zeek source="*conn.log" | table id.orig_h | dedup id.orig_h | sort id.orig_h</query>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </search>
      <default>__none__</default>
      <initialValue>__none__</initialValue>
      <valuePrefix>id.orig_h!="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> AND </delimiter>
      <prefix>(</prefix>
      <suffix>)</suffix>
    </input>
    <input type="multiselect" token="dst_port_include" searchWhenChanged="false">
      <label>Destination Port - Include</label>
      <choice value="*">All</choice>
      <fieldForLabel>id.resp_p</fieldForLabel>
      <fieldForValue>id.resp_p</fieldForValue>
      <search>
        <query>index=zeek source="*conn.log" | table id.resp_p | dedup id.resp_p | sort id.resp_p</query>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </search>
      <default>*</default>
      <initialValue>*</initialValue>
      <valuePrefix>id.resp_p="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> OR </delimiter>
      <prefix>(</prefix>
      <suffix>)</suffix>
    </input>
    <input type="multiselect" token="dst_port_exclude" searchWhenChanged="false">
      <label>Destination Port - Exclude</label>
      <choice value="__none__">None</choice>
      <fieldForLabel>id.resp_p</fieldForLabel>
      <fieldForValue>id.resp_p</fieldForValue>
      <search>
        <query>index=zeek source="*conn.log" | table id.resp_p | dedup id.resp_p | sort id.resp_p</query>
        <earliest>$time_range.earliest$</earliest>
        <latest>$time_range.latest$</latest>
      </search>
      <default>__none__</default>
      <initialValue>__none__</initialValue>
      <valuePrefix>id.resp_p!="</valuePrefix>
      <valueSuffix>"</valueSuffix>
      <delimiter> AND </delimiter>
      <prefix>(</prefix>
      <suffix>)</suffix>
    </input>
  </fieldset>

  <!-- ================================================================ -->
  <!-- ROW 1 — TTP OVERVIEW (notice.log BZAR/ATTACK:: alerts)           -->
  <!-- ================================================================ -->
  <row>
    <panel>
      <title>ATT&amp;CK Alerts Over Time (BZAR / notice.log)</title>
      <chart>
        <search>
          <query>index=zeek source="*notice.log" $src_ip_include$ $src_ip_exclude$
| search note="ATTACK::*" OR note="BZAR::*"
| timechart span=5m count by note limit=10</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="charting.chart">area</option>
        <option name="charting.chart.stackMode">stacked</option>
        <option name="charting.drilldown">all</option>
        <option name="refresh.display">progressbar</option>
      </chart>
    </panel>
    <panel>
      <title>Top ATT&amp;CK TTPs Fired (notice.log)</title>
      <chart>
        <search>
          <query>index=zeek source="*notice.log" $src_ip_include$ $src_ip_exclude$
| search note="ATTACK::*" OR note="BZAR::*"
| top limit=15 note</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="charting.chart">bar</option>
        <option name="charting.drilldown">all</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*notice.log"+note%3D"$click.value|u$"</link>
        </drilldown>
      </chart>
    </panel>
  </row>

  <!-- ================================================================ -->
  <!-- ROW 2 — RECONNAISSANCE &amp; DISCOVERY                           -->
  <!-- T1595 - Active Scanning | T1046 - Network Service Scan           -->
  <!-- T1018 - Remote System Discovery                                  -->
  <!-- ================================================================ -->
  <row>
    <panel>
      <title>T1595 / T1046 — Scanning: Hosts Probing Many Ports or Destinations</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$
| stats dc(id.resp_p) as unique_ports, dc(id.resp_h) as unique_hosts, count as connections by id.orig_h
| where unique_ports > 20 OR unique_hosts > 50
| eval TTP="T1595/T1046"
| sort - unique_ports</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1018 — Remote System Discovery: ICMP Host Sweeps</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$ proto=icmp
| stats dc(id.resp_h) as unique_targets, count as icmp_packets by id.orig_h
| where unique_targets > 10
| eval TTP="T1018"
| sort - unique_targets</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"+proto%3Dicmp</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <!-- ================================================================ -->
  <!-- ROW 3 — C2 APPLICATION LAYER PROTOCOLS                          -->
  <!-- T1071.001 - Web Protocols (HTTP POST beaconing)                  -->
  <!-- T1071.004 / T1572 - DNS C2 / Tunneling                          -->
  <!-- ================================================================ -->
  <row>
    <panel>
      <title>T1071.001 — HTTP C2 Beaconing (Repeated POSTs to External Hosts)</title>
      <table>
        <search>
          <query>index=zeek source="*http.log" $src_ip_include$ $src_ip_exclude$ method=POST
| where NOT match(id.resp_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| stats count as post_count, sum(request_body_len) as total_upload_bytes, dc(uri) as unique_uris, values(host) as hosts by id.orig_h, id.resp_h
| where post_count > 10
| eval total_upload_KB=round(total_upload_bytes/1024,1), TTP="T1071.001"
| fields - total_upload_bytes
| sort - post_count</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*http.log"+method%3DPOST+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1071.004 / T1572 — DNS Tunneling (High Query Volume or Long Subdomains)</title>
      <table>
        <search>
          <query>index=zeek source="*dns.log" $src_ip_include$ $src_ip_exclude$
| eval query_len=len(query)
| stats count as query_count, avg(query_len) as avg_query_len, max(query_len) as max_query_len, dc(query) as unique_queries by id.orig_h, id.resp_h
| where avg_query_len > 40 OR query_count > 500
| eval avg_query_len=round(avg_query_len,1), TTP="T1071.004/T1572"
| sort - query_count</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*dns.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>T1071.004 — DNS TXT / NULL / ANY Queries (Exfil/C2 Channel Indicators)</title>
      <table>
        <search>
          <query>index=zeek source="*dns.log" $src_ip_include$ $src_ip_exclude$
| where qtype_name="TXT" OR qtype_name="NULL" OR qtype_name="ANY"
| stats count as query_count, dc(query) as unique_queries, values(qtype_name) as types by id.orig_h, id.resp_h
| where query_count > 5
| eval TTP="T1071.004"
| sort - query_count</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*dns.log"+id.orig_h%3D"$row.id.orig_h|u$"+(qtype_name%3DTXT+OR+qtype_name%3DNULL)</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1571 — Non-Standard Port Usage (Protocol / Port Mismatch)</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$
| where (service="http" AND id.resp_p!=80 AND id.resp_p!=8080 AND id.resp_p!=8000 AND id.resp_p!=8443 AND id.resp_p!=8888)
   OR (service="ssl" AND id.resp_p!=443 AND id.resp_p!=8443 AND id.resp_p!=9443 AND id.resp_p!=465 AND id.resp_p!=993 AND id.resp_p!=995)
   OR (service="dns" AND id.resp_p!=53)
   OR (service="ssh" AND id.resp_p!=22)
   OR (service="smtp" AND id.resp_p!=25 AND id.resp_p!=587 AND id.resp_p!=465)
| stats count, sum(orig_bytes) as out_bytes, sum(resp_bytes) as in_bytes by id.orig_h, id.resp_h, id.resp_p, service
| eval out_KB=round(out_bytes/1024,1), TTP="T1571"
| fields - out_bytes, in_bytes
| sort - count</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"+id.resp_p%3D$row.id.resp_p|u$</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <!-- ================================================================ -->
  <!-- ROW 4 — ENCRYPTED CHANNEL &amp; INGRESS TOOL TRANSFER            -->
  <!-- T1573 - Encrypted Channel | T1105 - Ingress Tool Transfer        -->
  <!-- ================================================================ -->
  <row>
    <panel>
      <title>T1573 — Suspicious TLS Certificates (Self-Signed / Validation Failures)</title>
      <table>
        <search>
          <query>index=zeek source="*ssl.log" $src_ip_include$ $src_ip_exclude$
| where NOT match(id.resp_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| where subject=issuer OR validation_status!="ok" OR (isnull(server_name) AND isnull(subject))
| stats count as tls_sessions, dc(id.resp_h) as unique_servers, values(validation_status) as validation, values(subject) as subjects by id.orig_h, id.resp_h, id.resp_p
| eval TTP="T1573"
| sort - tls_sessions</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*ssl.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1105 — Ingress Tool Transfer (Executable / Script Downloads via HTTP)</title>
      <table>
        <search>
          <query>index=zeek source="*http.log" $src_ip_include$ $src_ip_exclude$
| where NOT match(id.resp_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| rex field=uri "\.(?P<ext>[a-zA-Z0-9]{2,5})(\?.*)?$"
| where match(ext, "(?i)^(exe|dll|bat|ps1|vbs|js|msi|scr|hta|cmd|jar|py|sh|elf|bin|com|pif|lnk|iso|img)$")
| stats count, values(ext) as file_types, values(uri) as uris, sum(response_body_len) as total_bytes by id.orig_h, id.resp_h, host
| eval total_KB=round(total_bytes/1024,1), TTP="T1105"
| fields - total_bytes
| sort - count</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*http.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <!-- ================================================================ -->
  <!-- ROW 5 — LATERAL MOVEMENT                                         -->
  <!-- T1021.001 - RDP | T1021.002 - SMB Admin Shares                  -->
  <!-- T1021.006 - WinRM | T1047 - DCE-RPC Named Pipes                 -->
  <!-- ================================================================ -->
  <row>
    <panel>
      <title>T1021.001 — RDP Lateral Movement Over Time (Port 3389)</title>
      <chart>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$ id.resp_p=3389
| timechart span=5m count AS "RDP Connections"</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="charting.chart">line</option>
        <option name="charting.drilldown">all</option>
        <option name="refresh.display">progressbar</option>
      </chart>
    </panel>
    <panel>
      <title>T1021.001 — RDP Source Hosts and Target Count</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$ id.resp_p=3389
| stats count as rdp_sessions, dc(id.resp_h) as unique_targets, sum(orig_bytes) as out_bytes by id.orig_h
| eval out_MB=round(out_bytes/1048576,2), TTP="T1021.001"
| fields - out_bytes
| sort - rdp_sessions</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"+id.resp_p%3D3389</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>T1021.002 — SMB Admin Share Access (ADMIN$, C$, IPC$, SYSVOL)</title>
      <table>
        <search>
          <query>index=zeek source="*smb_mapping.log" $src_ip_include$ $src_ip_exclude$
| where match(path, "(?i)(ADMIN\$|C\$|IPC\$|SYSVOL|NETLOGON)")
| stats count as mounts, dc(path) as unique_shares, dc(id.resp_h) as unique_targets, values(path) as shares by id.orig_h
| eval TTP="T1021.002"
| sort - mounts</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*smb_mapping.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1021.006 — WinRM Connections (Ports 5985 / 5986)</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$
| where id.resp_p=5985 OR id.resp_p=5986
| stats count as winrm_sessions, dc(id.resp_h) as unique_targets, sum(orig_bytes) as out_bytes by id.orig_h, id.resp_p
| eval out_KB=round(out_bytes/1024,1), TTP="T1021.006"
| fields - out_bytes
| sort - winrm_sessions</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"+(id.resp_p%3D5985+OR+id.resp_p%3D5986)</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>T1021.006 / T1047 — DCE-RPC High-Value Named Pipes (svcctl, samr, drsuapi, etc.)</title>
      <table>
        <search>
          <query>index=zeek source="*dce_rpc.log" $src_ip_include$ $src_ip_exclude$
| where match(endpoint, "(?i)(svcctl|samr|lsarpc|atsvc|winreg|drsuapi|epmapper|wkssvc|srvsvc|ITaskSchedulerService)")
| stats count as rpc_ops, dc(operation) as unique_ops, dc(id.resp_h) as unique_targets, values(endpoint) as pipes, values(operation) as operations by id.orig_h
| eval TTP="T1021.006/T1047"
| sort - rpc_ops</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*dce_rpc.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1021.006 / T1047 — DCE-RPC Named Pipe Activity Over Time</title>
      <chart>
        <search>
          <query>index=zeek source="*dce_rpc.log" $src_ip_include$ $src_ip_exclude$
| where match(endpoint, "(?i)(svcctl|samr|lsarpc|atsvc|winreg|drsuapi|epmapper|wkssvc|srvsvc)")
| timechart span=5m count by endpoint limit=10</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="charting.chart">column</option>
        <option name="charting.chart.stackMode">stacked</option>
        <option name="charting.drilldown">all</option>
        <option name="refresh.display">progressbar</option>
      </chart>
    </panel>
  </row>

  <!-- ================================================================ -->
  <!-- ROW 6 — EXFILTRATION                                             -->
  <!-- T1041 - Exfil over C2 Channel | T1048 - Exfil Alt Protocol      -->
  <!-- T1048.003 - DNS Exfil                                            -->
  <!-- ================================================================ -->
  <row>
    <panel>
      <title>T1041 — Large Outbound Transfers to External Hosts (High Ratio)</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$
| where NOT match(id.resp_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| where orig_bytes > 1000000
| eval ratio=if(resp_bytes>0, round(orig_bytes/resp_bytes,2), 9999)
| stats sum(orig_bytes) as total_out_bytes, sum(resp_bytes) as total_in_bytes, count as connections, avg(ratio) as avg_ratio by id.orig_h, id.resp_h, service
| eval total_out_MB=round(total_out_bytes/1048576,2), total_in_MB=round(total_in_bytes/1048576,2), avg_ratio=round(avg_ratio,2), TTP="T1041"
| fields - total_out_bytes, total_in_bytes
| sort - total_out_MB</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"+id.resp_h%3D"$row.id.resp_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1048.003 — DNS Exfil Candidates (Excessive Unique Subdomains per Host)</title>
      <table>
        <search>
          <query>index=zeek source="*dns.log" $src_ip_include$ $src_ip_exclude$
| rex field=query "(?P<subdomain>[^\.]+)\.(?P<base_domain>[^\.]+\.[^\.]+)$"
| where isnotnull(subdomain) AND len(subdomain) > 15
| stats count as query_count, dc(query) as unique_queries, dc(base_domain) as unique_domains by id.orig_h
| where unique_queries > 50
| eval TTP="T1048.003"
| sort - unique_queries</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*dns.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>T1048 — FTP Uploads to External Hosts (STOR Commands)</title>
      <table>
        <search>
          <query>index=zeek source="*ftp.log" $src_ip_include$ $src_ip_exclude$ command=STOR
| where NOT match(id.resp_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| stats count as uploads, sum(file_size) as total_bytes, values(arg) as filenames by id.orig_h, id.resp_h, user
| eval total_MB=round(total_bytes/1048576,2), TTP="T1048"
| fields - total_bytes
| sort - total_MB</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">10</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*ftp.log"+id.orig_h%3D"$row.id.orig_h|u$"+command%3DSTOR</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1048 — SMTP Exfiltration (Outbound Email with Attachments)</title>
      <table>
        <search>
          <query>index=zeek source="*smtp.log" $src_ip_include$ $src_ip_exclude$
| where NOT match(id.resp_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| eval has_attachment=if(isnotnull(fuids) AND fuids!="-" AND fuids!="", 1, 0)
| stats count as emails, sum(has_attachment) as with_attachments, values(mailfrom) as senders, values(rcptto) as recipients by id.orig_h, id.resp_h
| where with_attachments > 0
| eval TTP="T1048"
| sort - with_attachments</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">10</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*smtp.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <!-- ================================================================ -->
  <!-- ROW 7 — CREDENTIAL ACCESS                                        -->
  <!-- T1110 - Brute Force | T1558 - Kerberos Attacks                  -->
  <!-- ================================================================ -->
  <row>
    <panel>
      <title>T1110 — Auth Brute Force: Failures by Protocol (Kerberos / NTLM / SMB)</title>
      <table>
        <search>
          <query>index=zeek (source="*kerberos.log" OR source="*ntlm.log" OR source="*smb_cmd.log") $src_ip_include$ $src_ip_exclude$
| eval auth_failure=case(
    source like "*kerberos*" AND (success="false" OR success="F"), "Kerberos",
    source like "*ntlm*" AND (success="false" OR success="F"), "NTLM",
    source like "*smb_cmd*" AND status!="SUCCESS" AND status!="-" AND status!="", "SMB",
    true(), null())
| where isnotnull(auth_failure)
| stats count as failures, dc(id.resp_h) as unique_targets by id.orig_h, auth_failure
| where failures > 5
| eval TTP="T1110"
| sort - failures</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+(source%3D"*kerberos.log"+OR+source%3D"*ntlm.log")+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1110 — RDP Brute Force Candidates (Many Attempts per Hour)</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$ id.resp_p=3389
| bucket _time span=1h
| stats count as attempts, dc(id.resp_h) as unique_targets by _time, id.orig_h
| where attempts > 10
| eval TTP="T1110 - RDP Brute Force"
| sort - attempts</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"+id.resp_p%3D3389</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>T1558 — Kerberos Anomalies (Ticket Errors / Encryption Downgrade)</title>
      <table>
        <search>
          <query>index=zeek source="*kerberos.log" $src_ip_include$ $src_ip_exclude$
| where error_msg!="" AND error_msg!="-" AND isnotnull(error_msg)
| stats count as errors, dc(id.resp_h) as unique_kdc, values(error_msg) as error_types, values(cipher) as ciphers, values(request_type) as req_types by id.orig_h, client
| eval TTP="T1558"
| sort - errors</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*kerberos.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
    <panel>
      <title>T1133 — External Remote Services (Inbound RDP / SSH / WinRM from Outside)</title>
      <table>
        <search>
          <query>index=zeek source="*conn.log" $src_ip_include$ $src_ip_exclude$
| where NOT match(id.orig_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| where match(id.resp_h, "^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.)")
| where id.resp_p=3389 OR id.resp_p=22 OR id.resp_p=5985 OR id.resp_p=5986 OR id.resp_p=1194 OR id.resp_p=4500
| stats count as connections, dc(id.resp_h) as internal_targets, sum(orig_bytes) as in_bytes by id.orig_h, id.resp_p, service
| eval in_MB=round(in_bytes/1048576,2), TTP="T1133"
| fields - in_bytes
| sort - connections</query>
          <earliest>$time_range.earliest$</earliest>
          <latest>$time_range.latest$</latest>
        </search>
        <option name="count">15</option>
        <option name="drilldown">cell</option>
        <option name="refresh.display">progressbar</option>
        <drilldown target="_blank">
          <link>search?q=index%3Dzeek+source%3D"*conn.log"+id.orig_h%3D"$row.id.orig_h|u$"</link>
        </drilldown>
      </table>
    </panel>
  </row>

</form>
